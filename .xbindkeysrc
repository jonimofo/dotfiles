# Source : https://unix.stackexchange.com/questions/24330/how-can-i-turn-off-middle-mouse-button-paste-functionality-in-all-programs
# To deactivate middle click on Ubuntu 22.04
#
# To make it work :
# 	Install first xbindkeys xsel xdotool
# 	Run xbindkeys on startup


# When middle mouse button (b:2) is released:
# 1. Clear PRIMARY selection (so paste buffer is empty)
# 2. Kill xbindkeys to avoid recursion
# 3. Simulate a middle click (so apps still get the event)
# 4. Restart xbindkeys for the next trigger

"echo -n | xsel -n -i; pkill xbindkeys; xdotool click 2; xbindkeys"
b:2 + Release
