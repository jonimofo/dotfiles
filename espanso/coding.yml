matches:
  - trigger: ":td"
    replace: "// TODO REMOVE ME LATER"
    propagate_case: true

  - trigger: "#=="
    replace: |
      # ======================================================
      #   CHANGEME
      # ======================================================

