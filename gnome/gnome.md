## Backup/Restore gnome config
Backup gnome config:
```bash
dconf dump / > gnome_full_backup.dconf
```

Restore gnome config:
```bash
dconf load / < gnome_full_backup.dconf
```

## Extensions
- Clipboard indicator
- Just Perfection
